version: '2'
services:
  db:
    image: postgres
  web:
    environment:
      - RAILS_ENV=development
    build: .
    command: bash -c 'cp /myapp/config/database.docker.yml /myapp/config/database.yml && bundle exec rake db:setup && bundle exec rails s -p 3000 -b "0.0.0.0"'
    volumes:
      - .:/myapp
    ports:
      - "3000:3000"
    depends_on:
      - db
